// Phase 6 owns Extension endpoints. This module intentionally exports no HTTP
// operation until docs/api-contract.md defines and the gateway implements one.
export const extensionsAvailable = false;
